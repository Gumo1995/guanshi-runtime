# 更新说明

## v1.6.15

- 「日志复盘」升级为三维时间森林：以真实时间记录种树，可按月份查看、筛选待评分记录，并通过原有入口查看与评分。
- 新增卡通微缩林岛：原木小屋、池塘、山坡、棱面岩石与有纹理的土层；修复草地和土层交界处的白色缝隙。
- 支持自动慢转、操作后暂停、空闲20秒平滑归位；自动转动时镜头与光源固定。关闭旋转、查看详情或页面隐藏时暂停动画。
- 模型、引擎和贴图均随用户版打包，使用森林无需安装 Blender。
- 完成待办时，待办状态与实际时间记录通过同一持久化事务保存；保存失败可回滚，启动时恢复未完成事务，减少状态与记录不一致的问题。

## 之前版本的功能


- 新增完整六爻模块：本地无依赖排盘覆盖京房八宫、纳甲、六亲、六神、世应、伏神、旬空、本卦与变卦，并保留本地卦例；确定性卦盘事实不会交给模型重新计算。
- AI 对话可直接识别具体问卦，先判断问题类型和用神重点，再调用本地起卦工具生成卦盘并交给 Provider 解读；同卦追问按卦号隔离上下文，未选中时自动承接最近一卦，明确“另起一卦”时不会复用旧卦。
- Provider 调用按 Planner、通用 Writer 和六爻 Writer 分别治理输出预算、总超时、流空闲超时、停止、截断和有限恢复；排盘已完成但解读失败时会保留卦盘并明确显示未完成状态。
- 番茄钟升级为跨页面唯一悬浮组件，可拖动、键盘移动和收起；收起后嵌入顶部搜索栏右侧并上下对齐，展开时恢复原位置，指针长度缩短 20%。
- 质量与幸福感评分合并为一次选择的双维四象限控件，继续沿用原有 `quality` / `happiness` 字段以及番茄钟、日历的确认和保存链路。
- 待办拖拽与 AI 排程共享同一套本地约束、固定占用、依赖、拆分、最小变更和确认时复查规则；最终写入仍由原生待办链路一次完成。
- Planner、通用 Writer 和六爻 Writer 统一使用“观时 Personal Chief of Staff”人格契约，同时保持 Resolver、Workflow、确认和业务写入边界不变。
- AI 助手新增“完成待办并记录实际时间”：默认处理当前选中待办，可使用用户明确提供的实际日期、起止时间、质量/幸福评分以及标题、项目、分类、标签和备注修改。
- 实际时间和评分都不是必填项；缺少时间时会基于本地完成时刻和原预计时长生成带说明的待确认时间块，缺少评分时保持为空，不会虚构评价。
- 完成草稿确认后复用原生待办生命周期，标记一次性待办完成或完成本期周期待办，并生成对应的 `todo-completed` / `todo-recurring-completed` 日历实际记录；如已启用系统日历同步，继续沿现有安全链路处理。
- 待办列表与任务详情之间的整体间距调整为 16px，同时保留原有 12px 拖拽命中宽度。
- 待办只有跨过本地日期边界后才进入“已过期”；今天的事项即使计划时间已经过去，也继续留在今天。
- 待办拖拽结束后会立即刷新；拖到列表最底部空白区时，时间、项目和标签视图都会追加到视觉上最后一个有效分组的末尾。
- 多选待办现在可以通过 Delete 或 Backspace 一次删除，并继续执行每条待办原有的 Calendar、提醒、完成记录和番茄关联清理。
- 完成待办后会在原位置短暂停留再消失，不再先向下跳动一行。
- 待办行显示进一步收紧：备注改为小图标，锁定事项名称改为墨绿色加粗，持续时长/开始时间/日期集中到右侧，项目、提醒和标签集中到第三列。
- 完成圆点缩小并重新对齐，名称与信息列间距、上下视觉居中和第三列到时间数据的 20px 间距均已校准。
- 修复本地快照恢复反复显示“已取消恢复”的问题：现在首次点击会进入 15 秒应用内确认态，再次确认后才恢复，并保留恢复前备份。
- 自动同步不再因 Calendar 本次未返回事件而删除本地待办；空导出和异常批量删除会自动熄断，同步差异统一进入治理弹窗。
- 未来待办继续作为 Calendar 计划同步；过期未完成待办仅保留在本地“已过期”分组，已有的系统日历计划会自动清理，完成项目记录仍作为实际记录同步。
- v1.6.15 延续 v1.6 系列的稳定更新链，保留 Provider / BYOK、Domain Module Runtime 注册表、AI 记忆、确定性排程草稿、AI 工作流、Guanshi MCP 外部 Agent 接入和 AI QA 加固；所有数据变更仍需在观时内确认，默认不静默写待办、日历、提醒、卦例或 active memory。
- 本版本新增领域模块运行时基础：默认只启用 Time Module，并通过 module manifest、记忆命名空间、工具 scope、草稿 schema 和 apply policy 固定隔离边界；后续日记、记账、六爻等模块将以同一契约接入，不默认共享数据、记忆或写入权限。
- AI 对话编排现在基于模块工具 manifest 路由：Provider prompt 优先暴露 `time.*` 工具，旧的 `parse_task` 等 action 输出仍可兼容映射；未注册模块工具不会执行 workflow。
- 本版本完成 AI QA 加固：Provider 错误与非流式响应、MCP 输入/输出和持久化草稿记录都会进行敏感字段脱敏；MCP 工具增加 schema 校验，幂等 replay 不会覆盖用户已编辑的 pending 排程草稿或 memory proposal。
- 本版本新增 Guanshi MCP / 外部 Agent 接入基础层：Codex、Claude、Hermes 等客户端可通过受控本地工具读取最小上下文，并创建待确认任务草稿、排程草稿、memory proposal 和复盘洞察；`apply_draft` 仍禁用，不能静默改动待办、日历、提醒或 active memory。
- v1.5.17 已新增 AI 核心规划工作流入口：服务端可通过 `/api/ai/actions` 生成任务解析、任务拆解、时间管理原则探索、memory proposal、今日/本周计划意图、未完成任务重排建议和复盘洞察；所有输出仍停留在待确认 proposal 或 pending draft，不会自动改动待办、日历、提醒或记忆。
- v1.5.16 已新增 AI 记忆和确定性排程草稿基础层：服务端可保存待确认的 memory proposal、用户确认后的 active memory、scheduler 可读 projection，以及 pending schedule draft；排程草稿只用于预览和确认，不会自动改动待办、日历、提醒或记忆。
- v1.5.15 已新增 AI Provider / BYOK 本地基础层：服务端可保存 OpenAI-compatible、Anthropic、Hermes WebUI、Ollama、LM Studio 和自定义网关配置，并通过脱敏接口返回连接状态；chat / stream 路由必须显式确认外部请求后才会调用 Provider。
- v1.5.14 已完成 AI 架构与契约基础：AI 记忆、确定性排程草稿、MCP 外部 Agent 接入和中文 contract fixtures 已形成源码侧技术底座。
- 启动脚本默认使用 `127.0.0.1:8080`，也支持在设置页保存下次启动端口；端口被其他程序占用时会明确提示，不再静默切换到新端口。
- 左侧栏新增 AI 助手入口和占位面板，为后续规划今天、拆解任务、重排未完成等能力预留位置；本版本不会调用模型或自动改动数据。
- 总览页新增今日时间状态判断，优先显示宽松/正常/紧张/超载、最大截止风险、已安排时间、可用余量和待处理工作量。
- 待办数据新增向后兼容的规划字段，为后续优先级、剩余工作量、任务类型、拆分规则和 AI 建议记录做准备。
- 待办、日历记录、评分、分类等本地数据会在停止编辑 1 分钟后自动写入 `.runtime/local-data/latest.json`，并最多保留 10 条历史滚动快照。
- 如果浏览器当前端口下没有本地数据，观时会尝试从 `.runtime` 最新快照恢复，降低端口/origin 变化造成的数据不可见风险。
- 设置页现在可以浏览、预览、删除滚动快照并手动恢复指定快照；恢复前会自动保存当前数据，不会修改 macOS 日历或提醒事项。
- 在线更新遇到未提交改动时，可选择“备份并强制更新”。观时会将改动保存到 `.runtime/update-backups`，并提供导出副本与安全恢复入口。
- 用户版仍从公开仓库 `Gumo1995/guanshi-runtime` 的稳定 tag 更新。
- 运行缓存、日志、日历同步文件和数据快照继续放在隐藏 `.runtime` 目录里。
