# 更新说明

## v1.5.8

- 修复部分 macOS 机器 Command Line Tools / Swift SDK 不匹配时，刷新同步直接失败的问题。
- 当 Calendar Swift 脚本遇到 SDK/compiler 不匹配、`SwiftBridging` 重定义或 EventKit/Foundation 模块构建失败时，自动改走系统日历 JXA 兜底通道。
- 用户版仍从公开仓库 `Gumo1995/guanshi-runtime` 的稳定 tag 更新。
- 运行缓存、日志和日历同步文件继续放在隐藏 `.runtime` 目录里。
